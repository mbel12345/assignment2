from app.calculator import calculator

# Create the calculator is this is the actual program being run, rather than when it being called by another app
if __name__ == "__main__":

    # Star the REPL calculator
    calculator()
